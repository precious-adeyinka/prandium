import React from 'react'

// components
import Nav from '../Nav/NavIndex'

const HeaderIndex = () => {
    return (
        <header className="h-20 w-full flex justify-center items-center bg-transparent">
            <Nav />
        </header>
    )
};

export default HeaderIndex;